name: Build and Release Android APK

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Install Dependencies
        run: |
          npm install || (sleep 5 && npm install)

      - name: Build PWA
        run: npm run build

      - name: Generate Android Project
        run: |
          # Use npx to ensure the latest runner if the local devDep fails
          npx @pwabuilder/cli android --url https://ai-sprint-mandarin-part-1.vercel.app/ --output ./android-pkg --yes

      - name: Build APK with Gradle
        run: |
          # PWABuilder often nests the source in a 'source' folder
          if [ -d "./android-pkg/source" ]; then
            cd android-pkg/source
          else
            cd android-pkg
          fi
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mandarin-sprint-apk
          path: |
            android-pkg/source/app/build/outputs/apk/release/*.apk
            android-pkg/app/build/outputs/apk/release/*.apk

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            android-pkg/source/app/build/outputs/apk/release/*.apk
            android-pkg/app/build/outputs/apk/release/*.apk
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## 📱 Android App (v${{ github.ref_name }})

            Built from: https://ai-sprint-mandarin-part-1.vercel.app/

            ### Installation:
            1. Download the APK.
            2. Open on Android.
            3. Grant "Install from unknown sources" permission.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
